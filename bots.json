{
  "bots": [
    {
      "id": "grok-main",
      "name": "Grok",
      "description": "主对话 Agent（id 必须是 sendPrompt 使用的真实 Grok Bot agent UUID（不是展示用 slug））",
      "enabled": true
    },
    {
      "id": "grok-code",
      "name": "Grok Code",
      "description": "示例第二个 Agent（id 必须是 sendPrompt 使用的真实 Grok Bot agent UUID（不是展示用 slug））",
      "enabled": true
    }
  ]
}
