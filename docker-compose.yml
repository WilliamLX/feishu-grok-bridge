services:
  bridge:
    image: node:22-bookworm-slim
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: sh -c "npm ci && npm run build && npm start"
    restart: unless-stopped
